using System;

namespace StarForce {
    public enum OrderType  {
        NONE,
        GETINPUT,
        OUTPUT,
    };

    public enum InfoTypes {
        Error,
        Wram,
        Info,
    };

}