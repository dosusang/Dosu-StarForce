using System;

namespace StarForce {
    public enum OrderStatus {
        IS_BASE = 0,
        IN_AIR = 1,
        IN_HOLDER = 2,
    };

    public enum OrderMoveDirect {
        DOWN = 1,
        UP = -1,
    }

}